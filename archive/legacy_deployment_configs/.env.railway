# Railway Environment Variables Template
# Copy these to Railway Dashboard → Variables

# ==========================================
# REQUIRED - Add these in Railway Dashboard
# ==========================================

# Authentication & Security (GENERATE THESE!)
JWT_SECRET=GENERATE_WITH_openssl_rand_hex_32
EXCHANGE_KEY_ENCRYPTION_KEY=GENERATE_WITH_openssl_rand_base64_32

# Generate secure keys:
# JWT_SECRET: openssl rand -hex 32
# EXCHANGE_KEY_ENCRYPTION_KEY: openssl rand -base64 32

# ==========================================
# AUTO-CONFIGURED BY RAILWAY (Don't add these)
# ==========================================
# DATABASE_URL=postgresql://... (Railway auto-injects)
# REDIS_URL=redis://... (Railway auto-injects)
# PORT=... (Railway auto-injects)

# ==========================================
# OPTIONAL - For Full Features
# ==========================================

# Blockchain RPC URLs (use Alchemy free tier)
ETHEREUM_RPC_URL=https://eth-sepolia.g.alchemy.com/v2/YOUR_ALCHEMY_KEY
BASE_RPC_URL=https://base-sepolia.g.alchemy.com/v2/YOUR_ALCHEMY_KEY
ARBITRUM_RPC_URL=https://arb-sepolia.g.alchemy.com/v2/YOUR_ALCHEMY_KEY
POLYGON_RPC_URL=https://polygon-mumbai.g.alchemy.com/v2/YOUR_ALCHEMY_KEY

# DEX Aggregators (optional for DEX trading)
ZEROX_API_KEY=your-0x-api-key
OKX_DEX_API_KEY=your-okx-api-key
RUBIC_API_KEY=your-rubic-api-key

# CoinGecko (optional for price data)
COINGECKO_API_KEY=your-coingecko-api-key

# ==========================================
# APPLICATION SETTINGS (Optional)
# ==========================================

# Environment
NODE_ENV=production
LOG_LEVEL=INFO

# CORS (Railway backend URL)
CORS_ORIGINS=https://your-app.vercel.app,https://your-app.up.railway.app

# Rate Limiting
RATE_LIMIT_PER_MINUTE=60
RATE_LIMIT_BURST=100
RATE_LIMIT_ENABLED=true

# Features
ENABLE_TESTNET=true
ENABLE_REAL_MONEY_TRADING=false

# Data Retention (reduced for 1GB database)
MARKET_DATA_RETENTION_DAYS=30
TRADE_HISTORY_RETENTION_DAYS=90
LOG_RETENTION_DAYS=7

# ==========================================
# CELERY SETTINGS (Optional)
# ==========================================

# Celery uses REDIS_URL (auto-configured by Railway)
CELERY_BROKER_URL=${REDIS_URL}/1
CELERY_RESULT_BACKEND=${REDIS_URL}/2
CELERY_TASK_ALWAYS_EAGER=false

# ==========================================
# MONITORING (Optional)
# ==========================================

# Sentry (optional error tracking)
SENTRY_DSN=your-sentry-dsn
SENTRY_ENVIRONMENT=railway

# ==========================================
# NOTES
# ==========================================

# 1. NEVER commit this file with real values!
# 2. Add these directly in Railway Dashboard
# 3. Railway auto-configures DATABASE_URL, REDIS_URL, PORT
# 4. Use testnet RPCs for free testing
# 5. DEX APIs optional (app works without them)
