[tool:pytest]
# Options for pytest
testpaths = server_fastapi/tests
python_files = test_*.py *_test.py
python_classes = Test*
python_functions = test_*

# Async support
asyncio_mode = auto

# Coverage configuration - Target 90%+ coverage
# DISABLED: Coverage collection causes hangs; run tests first, then collect coverage separately
addopts = 