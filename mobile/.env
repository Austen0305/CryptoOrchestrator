# CryptoOrchestrator Mobile Environment
# Update these values for your environment

# API Configuration
# Your computer's IP address (auto-detected)
API_BASE_URL=http://10.0.0.22:8000
WS_BASE_URL=ws://10.0.0.22:8000

# IMPORTANT: Both your phone and computer must be on the same WiFi network!
# If the app can't connect, verify:
# 1. Backend is running: npm run dev:fastapi
# 2. Firewall allows port 8000
# 3. Both devices on same WiFi

# To update your IP if it changes:
# Windows: ipconfig (look for IPv4 Address)
# Mac: ifconfig | grep "inet " (look for 192.168.x.x)
# Linux: ip addr show (look for 192.168.x.x)

# App Configuration
APP_NAME=CryptoOrchestrator
APP_VERSION=1.0.0

# Feature Flags
ENABLE_BIOMETRICS=true
ENABLE_PUSH_NOTIFICATIONS=true
ENABLE_BACKGROUND_FETCH=true

# Debug
DEBUG_MODE=true
